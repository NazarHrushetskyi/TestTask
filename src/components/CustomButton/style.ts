import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  btn: {
    paddingVertical: 8,
    borderWidth: 1,
    borderRadius: 10,
    width: '50%',
    alignSelf: 'center',
    backgroundColor: 'rgb(2,0,16)',
    opacity: 0.9,
  },
  title: {
    fontSize: 22,
    color: '#fff',
    textAlign: 'center',
  },
});

export default styles;
